#!/bin/bash
set -euo pipefail
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)

"$SCRIPT_DIR/build-kernel"
"$SCRIPT_DIR/build-rootfs"
"$SCRIPT_DIR/build-initramfs"
"$SCRIPT_DIR/build-squashfs"
"$SCRIPT_DIR/build-iso"

if [ "${SKIP_QEMU:-0}" != 1 ]; then
    exec "$SCRIPT_DIR/test-qemu"
fi
